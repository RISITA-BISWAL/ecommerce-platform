pytest>=7.0.0
pandas>=2.0.0
numpy>=1.24.0
# Note: PySpark 3.5.4 is installed in the dedicated .venv_spark environment (Python 3.12)
# to maintain isolation from the primary .venv environment (Python 3.13).
pyspark>=3.0.0

